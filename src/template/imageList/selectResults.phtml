<?php   renderPaginationHiddenHTMLResults($pagination, $rowsFiltered, $rowsTotal) ?>

<?php
$yearMonthPrev = '';
foreach ($rows as $imgSlug => $html) {
    $mtime = $items[$imgSlug]['mtime'];
    $yearMonth = date('Ym', $mtime);
    if ($yearMonth != $yearMonthPrev) {
        echo '<h2>' . gFormatDate($mtime, 'MMMM y') . '</h2>' . PHP_EOL;
        $yearMonthPrev = $yearMonth;
    }
    echo $html;
}
?>

